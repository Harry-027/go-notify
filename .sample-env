SERVER_PORT=:3001
SERVER_URL=http://localhost:3001
DB_HOST=localhost
DB_PORT=5432
DB_USER=root
DB_PASSWORD=secret
DB_NAME=root
SECRET=secret
BROKER=localhost:9092
GROUP_ID=consumer-group-id
TOPIC=my-topic
MAILGUN_API_KEY=some-api-key // replace this with real credentials
MAILGUN_DOMAIN_NAME=mydomain.mailgun.org // replace this with real credentials
MAILGUN_FROM=<mailgun@mydomain.mailgun.org> // replace this with real credentials
KAFKA_PROTOCOL=tcp
JWT_SECRET=secret